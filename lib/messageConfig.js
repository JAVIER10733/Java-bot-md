/**
 * Java Bot MD - Configuración de Mensajes
 * 
 * NOTA DE SEGURIDAD: 
 * Este archivo ha sido neutralizado. El código ofuscado anterior inyectaba 
 * metadatos falsos de "Reenviado" (contextInfo) que violan las políticas 
 * de WhatsApp y pueden causar restricciones en el número.
 * 
 * Ahora utilizamos botones CTA oficiales (cta_url) en cada comando 
 * para promover el canal de forma legítima, segura y profesional.
 */

// Exportamos un objeto vacío para evitar que el bot crashee si algún 
// comando antiguo intenta importarlo, pero sin inyectar nada dañino.
module.exports = {
    channelInfo: {}
};